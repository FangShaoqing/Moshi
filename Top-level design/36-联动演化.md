# 36 · 情绪/缺点/持久化联动（她真正"演化"）

> 记录日期：2026-08-31（会话进行中）
> 状态：已实现并实测 ✅（混沌敏感保持 12.2 亿倍 / 降级正常 / 持久化联动通过）
> 目的：让"生气/缺点/被改变"不再是**一次性反应**，而是**持续演化的状态**（她真实地变化、跨会话延续）。

---

## 一、情绪状态联动（她真的会"被耗空"）

- **触发生气**（reaction.py）：真正冒犯 → `is_angry=True`，且**写进状态向量**（Valence↓0.15、Arousal↑0.12、Dominance↑0.08——真怒有生理/情绪痕迹）；
- **持续生气**（3 轮不消）→ **耗尽**：`exhausted=True`（不是怒，是**彻底凉了/没力气了**），状态 Valence↓0.12、Arousal↓0.10，反应转"沉默"（比怒更冷）；
- **emotion_state()**：从状态+生气/倦怠推导她当下整体情绪（供 LLM 理解她的"底色"）。

### 实测（✅ 情绪真演化——最打动人的部分）
```
闭嘴 →（抬眼）"我们还没熟到你能这么跟我说话。"  [生气 turns=1]
你好烦 →（沉默几秒）"……行，那我不说话了。"     [耗尽 exhausted=True]
你吧，就那样 →（没抬头，摩挲手机）"对，我就那样。你别管我了，我这样挺好。"  [彻底凉]
→ 情绪状态：她有点倦了，不是生气，是彻底凉了/没力气了；Valence=0.02（真被伤透）
```

## 二、缺点联动（她被长期对待后"变成不同的人"）

- **长期伤害**（hurt 累积 ≥3）→ **缺点加重**（+1 条："被人伤过之后，对类似的接近特别多疑，先防三分"）；
- **长期温暖**（warm 历史 ≥6）→ **软化缺点**（保留最轻一条——真实：被好好对待的人会慢慢圆润）；
- **独立计数** `_warm_history`（不依赖会被清零的 toward_you——修复了"软化永不触发"的 bug）。

### 实测（✅）
- 伤害 ×8 → 缺点 2→4（加重）；温暖 ×12 → 缺点 2→1（软化）。

## 三、持久化联动（她跨会话持续演化）

- `state.json` 存：toward_you / affected_beliefs / flaws / is_angry / exhausted / angry_turns；
- `load_person` 恢复——**她上次是"生气中/已耗尽/带某缺点"，下次还是**（她真是持续存在的）。

### 实测（✅）
- 保存（耗尽 + 缺点 2）→ 恢复（exhausted=True、flaws=2 完整）。

---

## 四、回归验证（✅）
生成 / 混沌敏感 12.2 亿倍 / 降级路径正常。

## 五、意义（她真正"活着"了）
现在她：**情绪会演化**（怒→耗尽→凉，写在状态里）、**缺点会被对待改变**（伤害加重/温暖软化）、**跨会话延续**（她带着上次的状态回来）——**不是一个"会做某事的反应器"，是一个持续演化、被你塑造、有状态的人**。

## 六、待后续
- 情绪与"生活"联动（她一天里的心境起伏，跨会话连续）；
- 关系网络；真实世界知识层；渠道接入。
