# ── 密钥与隐私（绝不提交）──
config/secrets.json

# ── 她的数据（含对你的记忆；一个种子一个宇宙）──
data/

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# ── 工具/日志 ──
.idea/
.vscode/
*.log
tmp_test_*.py
tmp_photo_*.py

# ── 本地 TTS 工程（CosyVoice 克隆 + 权重，几 GB，不入库）──
tmp_cosy/
.venv-cosy/
tmp_cosy_probe*.py
tmp_req*.txt

# ── 穿透工具（下载即用，不入库；silk_encoder.exe 是项目资产，保留）──
tools/cloudflared.exe
