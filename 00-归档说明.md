# 00 · 默识系统（ChenMoshi）项目说明

> 记录日期：2026-09-02（从"归档说明"更新为"项目说明"）
> 背景：项目曾发生哲学转向——新方向（默识AI / `../MoshiAI/`，坦诚哲学）与旧方向（极尽真实）分道扬镳，
> **但默识系统（ChenMoshi）没有放弃：仍然保留、仍在持续迭代**（当前：语音/本地 TTS 集成进行中）。
> 本目录与新系统完全隔离：代码、数据、配置、git 历史均独立，互不依赖。

## 一、本目录是什么

- 默识系统（v0–v4 全套 + QQ 通道 + 语音）的主目录：代码、设计文档 01–55、数据、配置、工具、模型、虚拟环境、**独立 git 仓库（含全部历史）**。
- 运行/接续上下文先读：`Top-level design/55-进度快照.md`（交接文档）。
- 哲学对照（供双轨并行时清醒）：旧系统=极尽真实（生命仿真/混沌演化）；新系统=坦诚（她是 AI，她知道，你也知道）。

## 二、怎么跑（迭代中，随时可跑）

```bash
# 在本目录内（注意 venv 是 3.11，不是默认的 3.14）
python -m moshi.qqbot --seed 20260901 [--sandbox] [--voice-url-base <公网>] [--voice-design C] [--no-voice] [--mode production]
python -m moshi.v4_demo [seed|--clear]
python -m moshi.whoami 20260827     # 形象卡
python -m moshi.voice_test [--candidate C] [文本]   # 语音试听
```

- 密钥：`config/secrets.json`（QQ_APPID/QQ_APP_SECRET/MIMO_API_KEY，真实值，**勿提交/勿外传**）。
- 她的数据：`data\SHE_20260901\`（同一 seed 跨会话延续）。
- 语音公网隧道：`tools\cloudflared.exe tunnel --url http://127.0.0.1:8902 --no-autoupdate`（URL 随重启变化）。
- 本地 TTS：`.venv-cosy`（CPython 3.11.15 + torch 2.3.1+cu121，RTX 2050 4GB fp32 可跑 CosyVoice2-0.5B）。

## 三、本机环境坑（血泪，勿重踩）

1. **系统代理**：Clash 在 `127.0.0.1:6478`，同时有 WinINET 系统代理。
   - `huggingface_hub` 0.36 的元数据 HEAD 只跟随**相对**重定向；经代理访问 hf-mirror 会得到 **308 绝对 Location**（指向 huggingface.co）→ `FileMetadataError`。
   - **官方 huggingface.co 直接走代理可用**（返回相对重定向）→ 下载模型用官方源即可，**不要**设 HF_ENDPOINT=hf-mirror + NO_PROXY 直连（直连镜像会 connect timeout）。
2. git 全局代理 `http://127.0.0.1:6478`（github 通，直连 curl 不通）。
3. CosyVoice 运行时：`inference_zero_shot` 按句 yield 必须 `torch.cat`；提示音频传**路径**；submodule `third_party/Matcha-TTS` 先 `git submodule update --init`。
4. 默认 `python` 是 3.14.6，CosyVoice 一律用 `.venv-cosy\Scripts\python.exe`。

## 四、当前迭代事项

- **语音/本地 TTS 集成进行中**：CosyVoice3-0.5B 已下载齐全（`tmp_cosy\pretrained_models\CosyVoice3-0.5B`，待集成/对比）；
- 情绪→声音映射、QQ 语音稳定性（隧道临时地址）、图片消息、真实世界知识层、正式测试套件。

## 五、与新系统（MoshiAI）的关系

- 双轨并行：ChenMoshi 继续老哲学迭代；MoshiAI 从零走新哲学，二者不共享代码/数据/配置/git。
- 跨系统可借鉴的资产（如有需要横向引用）：混沌性格生成思路（旧混沌内核 vs 新性格钻石）、语音链路（edge-tts/MiMo/CosyVoice 探针）、QQ 通道实现。
